tinymce.PluginManager.add('divLeftServicios', function(editor, url) {
    // Add a button that opens a window
    editor.addButton('customDivLeftElementButton', {
        text: 'Div Left',
        icon: false,
        onclick: function() {
            // Open window
            editor.windowManager.open({
                title: 'Please input text',
                body: [
                    {type: 'textbox', name: 'description', label: 'Text'}
                ],
                onsubmit: function(e) {
                    // Insert content when the window form is submitted
                    editor.insertContent('<div class="service-left">' + e.data.description + '</div>');
                }
            });
        }
    });

    // Adds a menu item to the tools menu
    editor.addMenuItem('customEmElementMenuItem', {
        text: 'Div Left Element',
        context: 'tools',
        onclick: function() {
            editor.insertContent('<div class="service-left">&nbsp;</div>');
        }
    });
});